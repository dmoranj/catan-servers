var
    config = {};

config.endpoint = {
    port: 3001,
    edit: true
};

config.mongo = {
    host: "localhost",
    db: 'catan-servers',
    user: '',
    password: ''
};

module.exports = config;